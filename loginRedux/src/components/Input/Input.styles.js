import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        padding:5,
        margin:10,
        borderRadius:10,
        backgroundColor:"white"
    },
    error:{
        color:'red',
        fontWeight:'bold',
        fontSize:16
    }
});